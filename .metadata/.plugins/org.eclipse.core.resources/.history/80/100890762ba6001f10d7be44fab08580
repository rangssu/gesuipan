<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>list</title>
</head>
<body>
    <h1 style="text-align: center;">게시판</h1>
    <div style="text-align: center; margin-bottom: 20px;">
        <a href="/gesuipan/write">새 글 작성</a>
    </div>
    <table>
        <thead>
            <tr>
                <th>번호</th>
                <th>제목</th>
                <th>작성자</th>
                <th>작성일</th>
                <th>관리</th>
            </tr>
        </thead>
        <tbody>
            <tr th:each="post : ${posts}">
                <td th:text="${post.id}"></td>
                <td>
                    <a th:href="@{/gesuipan/{id}(id=${post.id})}" th:text="${post.title}"></a>
                </td>
                <td th:text="${post.writer}"></td>
                <td th:text="${post.createdDate}"></td>
                <td>
                    <a th:href="@{/gesuipan/{id}/edit(id=${post.id})}">수정</a> |
                    <a th:href="@{/gesuipan/{id}/delete(id=${post.id})}" onclick="return confirm('정말 삭제하시겠습니까?')">삭제</a>
                </td>
            </tr>
            <tr th:if="${#lists.isEmpty(posts)}">
                <td colspan="5">게시글이 없습니다.</td>
            </tr>
        </tbody>
    </table>
</body>
</html>