package com.example.Gesuipan.service;

import com.example.Gesuipan.DTO.panDTO;
import org.springframework.stereotype.Service;

@Service
public class panService {
    // 게시물 ID로 조회하는 메서드
    public panDTO getPostById(int id) {
        // 실제 로직 구현 (예시)
        return new panDTO(); // 예시로 반환
    }
    
    // 게시물 저장하는 메서드
    public void savePost(panDTO post) {
        // 실제 로직 구현 (예시)
    }
    
    // 게시물 업데이트하는 메서드
    public void updatePost(panDTO post) {
        // 실제 로직 구현 (예시)
    }
    
    // 게시물 삭제하는 메서드
    public void deletePost(int id) {
        // 실제 로직 구현 (예시)
    }
}
